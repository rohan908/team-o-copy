{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "tasks": {
        "dev": {
            "env": ["PORT", "BACKEND_PORT", "BACKEND_SOURCE"]
        },
        "deploy": {
            "env": ["PORT"]
        }
    }
}
